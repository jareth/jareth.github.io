<?xml version="1.0" encoding="UTF-8"?>
<task>
  <meta>
    <id>06</id>
    <name>GitHub Actions Deployment</name>
    <phase>6</phase>
    <status>pending</status>
    <dependencies>04</dependencies>
  </meta>

  <description>
    Set up GitHub Actions workflow for automated builds and deployment.
  </description>

  <steps>
    <step number="6.1">
      <action>Create GitHub Actions workflow</action>
      <file>.github/workflows/deploy.yml</file>
      <content><![CDATA[
name: Build and Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build Eleventy
        run: yarn run build:html

      - name: Build CSS (production)
        run: yarn run build:css

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
]]></content>
    </step>

    <step number="6.2">
      <action>Configure GitHub Pages to use Actions</action>
      <type>manual</type>
      <instructions>
        <instruction>Go to repository Settings > Pages</instruction>
        <instruction>Under "Build and deployment", select "GitHub Actions"</instruction>
      </instructions>
    </step>
  </steps>

  <verification>
    <check>deploy.yml file exists in .github/workflows/</check>
    <check>GitHub Pages source is set to GitHub Actions</check>
    <check>Push to master triggers workflow</check>
    <check>Build completes successfully</check>
  </verification>
</task>
